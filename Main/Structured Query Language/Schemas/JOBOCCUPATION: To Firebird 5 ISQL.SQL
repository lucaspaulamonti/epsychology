CREATE TABLE JOBOCCUPATION (
    ID INTEGER,
    DESCRIPTION VARCHAR(75),
    CONSTRAINT PK_JOBOCCUPATION__ID PRIMARY KEY (ID),
    CONSTRAINT UQ_JOBOCCUPATION__DESCRIPTION UNIQUE (DESCRIPTION),
    CONSTRAINT NN_JOBOCCUPATION__DESCRIPTION CHECK (DESCRIPTION IS NOT NULL)
);

CREATE SEQUENCE SQ_JOBOCCUPATION__ID;
ALTER SEQUENCE SQ_JOBOCCUPATION__ID RESTART WITH 1;

SET TERM ^ ;

CREATE OR ALTER TRIGGER BI_JOBOCCUPATION__ID FOR JOBOCCUPATION
BEFORE INSERT
AS
BEGIN
  IF (NEW.ID IS NULL) THEN
    NEW.ID = NEXT VALUE FOR SQ_JOBOCCUPATION__ID;
END^

SET TERM ; ^

INSERT INTO JOBOCCUPATION (DESCRIPTION) VALUES ('DESEMPREGADO');
INSERT INTO JOBOCCUPATION (DESCRIPTION) VALUES ('ESTUDANTE');
INSERT INTO JOBOCCUPATION (DESCRIPTION) VALUES ('DO LAR');

COMMIT WORK;
